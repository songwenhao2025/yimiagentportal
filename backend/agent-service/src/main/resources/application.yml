server:
  port: 8082

spring:
  application:
    name: agent-service
  datasource:
    url: jdbc:mysql://10.206.20.129:3306/yimi_ai_portal?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 0AHPeevq2U2xWTi*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    com.yimi.ai: DEBUG
    org.springframework: INFO

llm:
  service:
    url: http://localhost:8087